export const TOKENS = {
  DEFAULT_EXPIRES_IN_SECONDS: 24 * 60 * 60,
  ACCESS_TOKEN_EXPIRES_IN_SECONDS: 24 * 60 * 60,
  JSON_WEB_TOKEN_SECRET: 'as82nask(ddks09dkwl29fkdikdf',
  REFRESH_TOKEN_EXPIRES_IN_SECONDS: 3 * 24 * 60 * 60,
  HTTP_ONLY_COOKIE_EXPIRES_IN_SECONDS: 3 * 24 * 60 * 60,
};

export const HASHES = {
  SALT_ROUNDS: 15,
};

export const KEYS = {
  COMPANY_KEY: 'verizon101',
};

export const USERS = {
  USERNAME: 10,
  EMAIL: 35,
  PASSWORD_MIN: 10,
  PASSWORD_MAX: 60,
};

export const REPORTS = {
  report: 350,
};

export const METRICS = {
  METRICS_NAME: 20,
  METRICS_UNIT: 10,
};
