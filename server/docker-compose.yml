services:
  nestjs:
    build: ./
    container_name: nestjs
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: B3nnywashere
      DB_NAME: verizon_app
    ports:
      - '5000:5000'
    depends_on:
      - postgres
    networks:
      - nest-network

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: verizon_app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: B3nnywashere
    volumes:
      - /var/lib/postgresql/data
    networks:
      - nest-network

networks:
  nest-network:
    driver: bridge
